server:
  port: 8012

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: # browser url for the git repo
          username: # git username
          password: # personal token
          clone-on-start: true
          default-label: main

